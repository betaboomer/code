#include <bits/stdc++.h> 
using namespace std; 
 
typedef long long ll;

const int k = 998244353;

int main() { 
	ios_base::sync_with_stdio(0);
    cin.tie(0);
 
    int t; cin >> t;
    while(t--){
        int n; cin >> n;

        vector<int> a(n);
        for (int i = 0; i < n; i++) cin >> a[i];

        
    }
} 
