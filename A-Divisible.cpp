#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int n, a, b; cin >> n >> a >> b;

    vector<ll>d(n);
    for (int i = 0; i < n; i++) cin >> d[i];

   
}
